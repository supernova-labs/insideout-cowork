.DS_Store
Thumbs.db

# Estado local do Claude Code CLI (não versionar)
.claude/

# core/ — segredos e artefatos de runtime (NUNCA commitar)
**/core/.env
**/core/outputs/
**/core/.image_session.json

# painel unificado gerado na pasta de trabalho (estilos × produtos × grid):
#   é derivado (regenerado dos JSONs) — não versionar
insideout-painel.html

# dados das três bibliotecas (versionar o ativo, ignorar só a lixeira):
#   style-gallery: versionar styles/ e thumbnails/
#   product-catalog: versionar brands/ products/ photos/
#   grids: versionar <marca>/*.json, rules/*.md, calendar/*.md, mockups/
**/style-gallery/.trash/
**/product-catalog/.trash/
**/grids/.trash/

**/__pycache__/
*.pyc
*.pyo

# Scratch de automação de browser / verificação visual (Playwright MCP)
.playwright-mcp/
page-*.jpeg
page-*.png
